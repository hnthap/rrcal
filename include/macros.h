#ifndef RRCAL_MACROS_H
#define RRCAL_MACROS_H

/**
 * @brief Pre-defined max number of roots (for static array of roots).
 * 
 * This macro is mandatory, i.e. this must not be commented out.
 */
#define RRCAL_MAX_ROOT_COUNT (256)

/**
 * @brief Pre-defined max length of the string representation of a polynomial.
 * 
 * This macro is mandatory, i.e. this must not be commented out.
 */
#define RRCAL_POLYNOMIAL_STRING_MAX_LENGTH (1000)

#endif  /** RRCAL_MACROS_H */